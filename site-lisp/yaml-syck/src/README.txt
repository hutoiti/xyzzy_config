= INSTALL

dll をビルドするには以下の環境が必要。

  - msys
  - mingw
  - wget


= BUILD

以下のコマンドで syck.dll と xyck.dll ができる。

  % make


= DLL

== syck.dll

  syck が提供する API。

== xyck.dll

  xyzzy からアクセスしたいけど syck.dll には API が用意されていないものへの
  API を提供する。
